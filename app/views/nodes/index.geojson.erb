<%= Yajl::Encoder.encode({:type => 'FeatureCollection', :bbox => [], :features => @places.map{|node| node_hash = node.to_geojson; node_hash[:properties].update({:marker => image_path(node.marker), :icon => image_path(node.icon)});node_hash}}, :pretty => Rails.env.development?).html_safe %>