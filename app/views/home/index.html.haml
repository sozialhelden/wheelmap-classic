#sidebar
  = image_tag('close.gif', :size => '17x17', :alt => 'x', :class => 'minimize')
  = render :partial => 'home/wheelchair_status_filter'
  %div.minimize
    &nbsp;
  = render :partial => 'home/category_filter'
  = link_to "http://itunes.apple.com/#{I18n.locale}/app/wheelmap/id399239476?mt=8" do
    =image_tag("/images/app_store_badge.png", :size => '134x45')
  = link_to "https://market.android.com/details?id=org.wheelmap.android.online&hl=#{I18n.locale}" do
    =image_tag("/images/market_store_badge.png", :size => '129x45')
  = link_to("http://thebobs.com/deutsch/category/2012/best-use-of-technology-for-social-good-2012/#n3055", :target => "_blank", :style => "cursor: pointer; background: url(https://thebobs.com/banner/custom_300x50.png); width: 300px; height: 50px; display: inline-block; text-decoration: none; font-weight: bold; font-face: Arial; font-size: 10px;") do
    %div{:style => "cursor: pointer; text-align: center; font-style: italic; color: #0087eb; text-decoration: none; margin: 25px 0px 0px 150px; width: 140px; color: #FFFFFF;"}Wheelmap
    %div{:style => "cursor: pointer; color: #FFFFFF; position:relative; top: -28px; text-align: center; text-transform: uppercase; font-style: normal; line-height: 17px; width: 142px; left: 150px; font-size: 10px;"}Vote for:
#spinner

#facebox
  %h2=t('.ie.warning.headline')
  .content
    %p=t('.ie.warning.message')

    %p
      = link_to t('.ie.action.upgrade'), "http://www.updateyourbrowser.net/en/", :class => 'button primary'
      = link_to t('.ie.action.ignore'), '#', :class => 'button secondary close', :onclick => 'return false;'

-content_for :javascript do
  :javascript
    drawmap(defaultControls(), 'map');
    createPlacesLayer(placesStyle());
    initGeoData();
    $(function () {
      jumpTo(lon,lat,zoom);
    })

    if($.cookie('ie_warning') != 1 && $.browser.msie && parseInt($.browser.version) < 8){
      $("#facebox").overlay({
        top: 260,
        mask: {
          color: '#cccccccc', loadSpeed: 200, opacity: 0.5
        },
        closeOnClick: false,
        load: true
      });
      $.cookie('ie_warning', 1);
    }

  = render :partial => 'shared/google_event_tracking'
  = render :partial => 'shared/google_view_tracking'