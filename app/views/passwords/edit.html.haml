%h2=t('devise.password_reset.headline')

- form_for resource_name, resource, :url => password_path(resource_name), :html => { :method => :put } do |f|
  = f.error_messages
  = f.hidden_field :reset_password_token

  %p= f.label :password, t('devise.password_reset.form.labels.password')
  %p= f.password_field :password

  %p= f.label :password_confirmation, t('devise.password_reset.form.labels.password_confirmation')
  %p= f.password_field :password_confirmation

  %p= f.submit t('devise.password_reset.form.submit')


= render :partial => "shared/devise_links"