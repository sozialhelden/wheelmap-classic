= render layout: 'profile' do
  .tab-pane.active#images
    %h1= t('.headline.your_photos')
    #thumbs_private2
      - unless @photos.empty?
        %ul.thumbnails
          - @photos.each do |photo|
            %li.span3{style: 'margin-left:20px;'}
              = link_to_photo(photo)
        .center
          = paginate @photos
      - else
        %p Lad deine ersten Photos hoch
