{{! header }}
<header class="node-popup-header headline-gradient">
  <h2 itemprop="name">{{headline}}</h2>
  {{#if address}}
    <address>{{address}}</address>
  {{/if}}

  <ol class="breadcrumbs unstyled">
    {{#each breadcrumbs}}
      <li><i class="icon-angle-right"></i>{{this}}</li>
    {{/each}}
  </ol>
</header>
